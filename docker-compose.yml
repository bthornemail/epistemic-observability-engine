version: '3.8'

services:
  epistemic-engine:
    build: .
    ports:
      - "8080:8080"
    environment:
      - EPISTEMIC_PORT=8080
      - EPISTEMIC_LOG_LEVEL=info
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped


